# CLAUDE.md — 孫八(mago8.jp)公式サイト 作業ルール

このリポジトリで作業するすべてのAIは、作業を始める前に必ずこのファイルを読むこと。
オーナーの明示的な指示を除き、ここに書かれたルールを優先する。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| 宿の実態 | 滋賀県米原市梓河内344(〒521-0211)の築120年 古民家一棟貸し宿 |
| 本番URL | https://mago8.jp/ |
| リポジトリ | mago8hotel/shiga |
| 公開方法 | GitHub Pages。**`main` ブランチがそのまま本番** |
| 主要ファイル | `index.html`(トップページ全体。CSS・JSもこの1ファイルに内包) |
| 別ツール | `salary/`(清掃員向けの給料計算ツール。**触らない**。§2参照) |
| 予約 | Beds24 https://beds24.com/booking2.php?propid=344052&lang=ja |
| 画像生成 | `scripts/optimize_images.py`(Pillow 11.3.0)。配信用WebPは `assets/images/` |
| 経緯メモ | `docs/image-delivery-optimization.md` |

`main` への変更は即座に本番へ反映される。取り返しがつかないため、後述の作業ルールを厳守すること。

---

## 2. 絶対に変更してはいけないもの

変更依頼を受けても、オーナー本人が「この値を変える」と明示していない限り触らない。

- **Beds24の `propid=344052`**
  通常の予約ボタンと固定CTAの両方。リンク先が変わると予約が別施設に飛ぶ。
- **canonical / OGP / JSON-LD の本番URL `https://mago8.jp/`**
  第1フェーズでこのURLに統一済み。末尾スラッシュを含めて変更しない。
- **郵便番号 `521-0211`、住所「滋賀県米原市梓河内344」**
- **チェックイン 15:00 / チェックアウト 11:00**
- **`salary/` ディレクトリ(清掃員向けの給料計算ツール)**
  トップページとは別の、公開中の業務用ツール。サイト改善の作業対象ではない。
  変更・削除・移動・リンク追加をしない。画像最適化スクリプト等の一括処理の対象にも含めない。

---

## 3. 禁止事項

### 情報の取り扱い
- **確認できない施設情報を推測で書かない。**
  座標(geo)・料金(priceRange)・支払方法(paymentAccepted)・部屋数(numberOfRooms)・
  レビュー件数や評価などは、オーナーから提示された事実以外書かない。
  これらは第1フェーズで「未確認のため削除」した経緯がある。同じものを復活させない。
- **ページに表示されていない内容を構造化データだけで作らない。**
  JSON-LDはページ上の記載と一致していること。第1フェーズでFAQ構造化データを削除済み。
- **SEO目的の不自然な文章・キーワード詰め込み・隠しテキストを入れない。**

### Git操作
- **`main` への直接pushは禁止。**
- **force push は禁止**(`--force` / `--force-with-lease` とも)。
- **マージはしない。** PRを作成した時点で停止する。マージ可否は人間が判断する。

### スコープ
- **指示された範囲外のデザイン・文言変更をしない。**
  「ついでに直しておいた」は禁止。気づいた点は実装せずPR本文かコメントで報告する。

---

## 4. 作業ルール

1. **必ず作業ブランチを切る。** `git checkout -b <branch> origin/main` で最新のmainから分岐する。
2. **PRを作成して停止する。** mergeは人間の判断。レビューは別セッションのClaudeが独立して行う。
3. **1PR = 1目的。** スコープを広げない。別の改善を思いついたら別PRにする。
4. **変更後は 1440px / 820px / 375px の3幅で表示確認する。**
   レイアウト崩れ、横スクロール、画像切れ、画質劣化をそれぞれ確認する。
5. **写真は宿の商品。拡大表示(ライトボックス)用の高解像度画像は必ず維持する。**
   配信用に縮小・WebP化した画像を作っても、`data-full` が指す元のJPEGは削除・差し替えしない。
6. **PR本文には必ず書く**:目的 / 変更ファイル一覧 / 実装方法 / 検証結果 / 未検証事項。
   画像を変更した場合は変更前後のバイト数を明記する。
7. **測定値を推測で書かない。** PageSpeed等が取得できなかった場合は「未測定」と明記する。

---

## 5. 現在の状態(完了済み)

### 第1フェーズ(本番反映済み)
- canonical / OGP / JSON-LD / sitemap.xml / robots.txt を `https://mago8.jp/` に統一
- 郵便番号を 521-0211 に修正
- 未確認の geo / priceRange / paymentAccepted / numberOfRooms を削除
- FAQ構造化データを削除(ページ上に対応する記載がないため)
- H1「古民家宿」の表示を復元
- 動画のレイアウトシフトを修正
- スマホの口コミ幅、820pxでのメニュー/魅力カードの表示を修正

### 第2フェーズ(PR #1、merge commit `b3d1250`)
- サムネイル18枚を216px角/240px角のWebP化(合計 4,922,564 → 262,400 bytes)
- トップ背景・魅力カード背景・aboutの写真をWebP化(`assets/images/*.webp`)
- ロゴを 342px / 780px のWebPに縮小して配信
- **拡大表示は元の高解像度JPEGを維持**(`data-full` 属性)
- 生成スクリプト `scripts/optimize_images.py` を追加

---

## 6. 既知の未解決事項

着手する場合は必ず個別のPRにすること。ついでに直さない。

- **初期アニメーション中の一時的な横幅超過**
  375px幅で一瞬ページ幅が約390pxになる。原因はトップ背景の `scale(1.08)`
  (`.hero::before` の `heroZoom` アニメーション)。アニメーション終了後は375pxに戻る。
- **iPhone実機の safe-area 未検証**
  ノッチ/ホームインジケータ周りは実機で未確認。ブラウザ幅での確認のみ。
- **GA4等の計測が未導入**
  アクセス解析タグは入っていない。改善効果は PageSpeed Insights の単発測定でしか見られない。
- **WebP非対応ブラウザ向けのフォールバックなし**
  現行ブラウザのみを対象としている。
